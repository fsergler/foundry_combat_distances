# templates/CLAUDE.md

## config.html

Handlebars template for the `CombatDistancesConfig` application. Receives `ranges` from `_prepareContext()` — an array of ring objects with `distance`, `label`, and `color` fields. Renders a form where users can add, remove, and edit rings.
